import 'jest-canvas-mock';
import 'jest';
beforeAll(() => {
});
beforeEach(() => {
 
});
afterEach(() => {
});
afterAll(() => {
});
describe('Unit Tests Game Class', () => {
  test('General Perperties', () => {
  
  });
  
});
